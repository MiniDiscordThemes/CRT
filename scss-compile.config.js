/** @type {import('bd-scss/lib/config').Config} */
export default {
    meta: {
        name: "CRT" /* The official name of your theme. */,
        scss: "crt" /* The name of your base scss file. */,
        repo: "CRT" /* The name of your theme repository. */,
        version: "dev" /* The version of your theme. */
    }
};
